pipeline {
    agent {label 'workers'}
    stages {
        stage('Spellcheck') {
            steps {
                sh '''
                chmod +x ./scripts/spellcheck.sh
                bash ./scripts/spellcheck.sh
                '''
            }
        }
    }
}
